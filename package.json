{
  "name": "ogrenci-api-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Öğrenci API projesi için kök çalışma komutları",
  "scripts": {
    "dev": "npm --prefix Backend/ogrenci-api run dev",
    "build": "npm --prefix Backend/ogrenci-api run build",
    "typecheck": "npm --prefix Backend/ogrenci-api run typecheck",
    "test": "npm --prefix Backend/ogrenci-api test",
    "check": "npm --prefix Backend/ogrenci-api run check",
    "start": "npm --prefix Backend/ogrenci-api start",
    "db:sync-indexes": "npm --prefix Backend/ogrenci-api run db:sync-indexes"
  },
  "engines": {
    "node": ">=20"
  }
}
